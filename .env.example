# Local development defaults. Copy to .env and change values as needed.
SECRET_KEY=dev-only-insecure-secret-key
DEBUG=1
ALLOWED_HOSTS=127.0.0.1 localhost
BASE_URL=http://127.0.0.1:8000

DB_ENGINE=django.db.backends.postgresql
DB_NAME=musicshowwins
DB_USER=musicshowwins
DB_PASSWORD=musicshowwins
DB_HOST=127.0.0.1
DB_PORT=5432

# Wikimedia asks clients to identify themselves. Use a real contact in shared environments.
WIKI_AGENT=musicshowwins/0.1 (local development)
